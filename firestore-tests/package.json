{
  "name": "mednu-firestore-rules-tests",
  "description": "Firestore Rules Unit Tests for the shared firestore.rules file — Lab & Diagnostics, and orders (Patient Prescription Upload).",
  "private": true,
  "scripts": {
    "test": "node --test lab.rules.test.js orders_prescription.rules.test.js appointments_join_call.rules.test.js guest_join.rules.test.js"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^3.0.4"
  },
  "engines": {
    "node": ">=18"
  }
}
